<%= link_to "Back", bookmarks_path %>

<%= form_for @category do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>
  <%= f.submit "Submit" %>
<% end %>

<ul>
  <% @categories.each do |cat| %>
    <li>
      <%= cat.name %>
    </li>
  <% end %>
</ul>
